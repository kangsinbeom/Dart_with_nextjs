"use client";

import HarfArrowIcon from "@/ui/icon/halfArrow";
import LogoIcon from "@/ui/icon/logo";

const LoginBox = () => {
  return (
    <div className="relative w-full max-w-[500px] center-position">
      <HarfArrowIcon styles="absolute left-0" />
      <LogoIcon />
    </div>
  );
};

export default LoginBox;
